name: Sync D1 Data(user)

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository (full history)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y jq curl gh

      - name: Fetch and update D1 data
        env:
          ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          DATABASE_ID: ${{ secrets.CLOUDFLARE_D1_DATABASE_ID }}
          API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          echo "📦 正在从 Cloudflare D1 获取数据..."
          curl -s -X POST \
            "https://api.cloudflare.com/client/v4/accounts/$ACCOUNT_ID/d1/database/$DATABASE_ID/query" \
            -H "Authorization: Bearer $API_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"sql":"SELECT * FROM user"}' | \
          jq '.result[0].results' | jq '{list: .}' > public/userlist.json

          if [ "$(jq '.list | length' public/userlist.json)" -eq 0 ]; then
            echo "✅ 没有任何可比较的内容，运行正常，结束工作流。"
            exit 0
          fi

      - name: Compare and push changes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

          git fetch origin main

          OLD_FILE_EXISTS=0
          if git ls-tree -r origin/main --name-only | grep -q '^public/userlist.json$'; then
            mkdir -p old_public
            git show origin/main:public/userlist.json > old_public/userlist.json
            OLD_FILE_EXISTS=1
          fi

          if [ "$OLD_FILE_EXISTS" -eq 1 ] && cmp -s old_public/userlist.json public/userlist.json; then
            echo "✅ public/userlist.json 与 origin/main 相同，暂无变化，结束工作流。"
            exit 0
          fi

          BRANCH="d1-sync"
          git checkout -B "$BRANCH"
          git add public/userlist.json
          git commit -m "chore: sync D1 data ($(date +%Y-%m-%d))" || echo "ℹ️ 没有文件变化可提交"
          git push origin "$BRANCH" -f

      - name: Wait for branch sync
        run: sleep 5

      - name: Create or update Pull Request, then attempt auto-merge
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Optional: installation token for a GitHub App with merge rights
          GITHUB_APP_INSTALLATION_TOKEN: ${{ secrets.GITHUB_APP_INSTALLATION_TOKEN }}
        run: |
          set -euo pipefail
          BRANCH="d1-sync"
          LABEL="CloudflareD1 auto-sync"

          echo "🪄 检查是否已有 PR..."
          EXISTING_PR=$(gh pr list --head "$BRANCH" --state open --json number --jq '.[0].number' || true)

          echo "🔍 检查与 main 的差异..."
          if [ "$(git rev-list origin/main..origin/$BRANCH | wc -l)" -eq 0 ]; then
            echo "✅ main 与 $BRANCH 没有任何差异，跳过 PR 创建。"
            exit 0
          fi

          PR_NUM=""
          if [ -n "$EXISTING_PR" ]; then
            PR_NUM="$EXISTING_PR"
            echo "🔄 已存在 PR #$PR_NUM，更新分支完成。"
            gh pr edit "$PR_NUM" --add-label "$LABEL" || echo "⚠️ 无法添加标签，可能标签不存在。"
          else
            echo "✨ 创建新的 PR..."
            gh pr create \
              --title "Update D1 data ($(date +%Y-%m-%d))" \
              --body "Automated data sync from Cloudflare D1. I need @Iamliuxiaozhen to handle this pull request." \
              --base main \
              --head "$BRANCH"

            PR_NUM=$(gh pr list --head "$BRANCH" --state open --json number --jq '.[0].number' || true)
            if [ -n "$PR_NUM" ]; then
              echo "🏷️ 为新 PR #$PR_NUM 添加标签 $LABEL"
              gh pr edit "$PR_NUM" --add-label "$LABEL" || echo "⚠️ 无法添加标签，可能标签不存在。"
            fi
          fi

          if [ -z "$PR_NUM" ]; then
            echo "⚠️ 未能获取 PR 编号，退出。"
            exit 0
          fi

          echo "🔁 尝试使用 Action 身份合并 PR #$PR_NUM（不能绕过受保护分支的强制签名）。"
          # 首先尝试用 GH_TOKEN（actions bot）合并
          if gh pr merge "$PR_NUM" --merge --delete-branch --body "Auto-merged by workflow" >/dev/null 2>&1; then
            echo "✅ PR #$PR_NUM 已由 Action 身份合并。"
            exit 0
          fi

          echo "⚠️ Action 身份合并失败（可能是受保护分支或签名要求）。尝试使用 GitHub App 安装令牌回退合并（若提供）。"

          if [ -z "${GITHUB_APP_INSTALLATION_TOKEN:-}" ]; then
            echo "ℹ️ 未配置 GITHUB_APP_INSTALLATION_TOKEN。无法使用 GitHub App 合并，保持 PR 打开供人工处理。"
            exit 0
          fi

          # 使用 GitHub App 安装令牌通过 REST API 合并 PR。该安装令牌由你的 GitHub App 创建并放到仓库 Secrets 中（见下方说明）。
          API_URL="https://api.github.com/repos/${GITHUB_REPOSITORY}/pulls/$PR_NUM/merge"
          echo "PUT $API_URL"
          merge_payload='{"commit_title":"Auto-merge D1 sync","commit_message":"Automated data sync from Cloudflare D1.","merge_method":"merge"}'

          HTTP_CODE=$(curl -s -o /tmp/merge_resp -w "%{http_code}" -X PUT "$API_URL" \
            -H "Authorization: Bearer ${GITHUB_APP_INSTALLATION_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -d "$merge_payload")

          if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
            echo "✅ PR #$PR_NUM 已由 GitHub App 安装令牌合并。"
          else
            echo "❌ 使用 GitHub App 合并失败 (HTTP $HTTP_CODE)："
            cat /tmp/merge_resp || true
            echo "保留 PR 供人工处理或其他自动化工具处理。"
          fi