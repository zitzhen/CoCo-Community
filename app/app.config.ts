export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      secondary: 'violet',
      neutral: 'slate'
    },
    button: {
      slots: {
        base: 'cursor-pointer font-semibold'
      }
    }
  }
})
